plugins {
    id "com.android.application"
    // START: FlutterFire Configuration
    id 'com.google.gms.google-services'
    // END: FlutterFire Configuration
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.leafcare"
    compileSdk = 35
    ndkVersion "27.0.12077973" // ✅ Set correct static NDK version

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        applicationId = "com.example.leafcare"
        minSdkVersion 23             // ✅ Updated to fix Firebase issue
        targetSdkVersion 35
        versionCode = 1
        versionName = "1.0"
        multiDexEnabled true         // ✅ Optional: helpful if you exceed 64K methods
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}
//pp